$('<%= j(render chat_messages) %>').appendTo($(".chat .pane"));

// Update pagination link
<% if chat_messages.last_page? %>
  $('.pagination').html("That's all, folks!");
<% else %>
  $('.pagination').html("<%=j link_to_next_page(chat_messages, 'Next Page', :remote => true) %>");
<% end %>